dockerdoc:
  image: docs/archive:${DOCKER_DOC_VERSION}
  container_name: ${DOCKER_DOC_SUBDOMAIN}-${DOMAIN}
  environment:
    - VIRTUAL_HOST=${DOCKER_DOC_SUBDOMAIN}.${DOMAIN}
    - LETSENCRYPT_HOST=${DOCKER_DOC_SUBDOMAIN}.${DOMAIN}
    - LETSENCRYPT_EMAIL=${DOCKER_DOC_SUBDOMAIN}@${DOMAIN}
  ports:
    - ${DOCKER_DOC_WEB_PORT}:4000
